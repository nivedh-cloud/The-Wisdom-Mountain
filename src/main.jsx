import React from 'react';
import ReactDOM from 'react-dom/client';
// TODO: Replace with your actual root component
// import App from './App';

const Root = () => <div>App entry not found. Please add your main component.</div>;

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <Root />
  </React.StrictMode>
);
